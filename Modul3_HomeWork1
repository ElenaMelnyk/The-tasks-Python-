#Відомий РІК. Визначити, чи буде цей рік високосним, і до якого століття цей рік відноситься.

x = int (input ("Input a year "))
y = x // 100 +1

if x <= 0:
    print ("Error")
elif x % 4 == 0:
    print ("Not a leap year of the", y, "century")
else:
    print ("A leap year of the", y, "century")
