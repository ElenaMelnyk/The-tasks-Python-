print ("This program calculate the area and perimeter of the rectangle for a given width and height.")
a = int (input ("Input the width of the rectangle. A: "))
b = int (input ("Input the height of the rectangle. B: "))
while a <=0 or b <= 0:
    print ("Wrong!")
    a = int (input ("Input the width of the rectangle. A: "))
    b = int (input ("Input the height of the rectangle. B: "))
else:
    p = 2 * (a + b)
    s = a * b
    print ("The perimeter of the rectangle is: ", p)
    print ("The area of the rectangle is: ", s)
